enum Rarity { mytical, legend, epic, rare, uncommon, common }

enum FishingGuide { guide0, guide1, guide2 }

enum FishingState { idle, fishing, strike }

enum FishingCatchState { green, yellow, red }
