import 'package:flutter/material.dart';

class CollectionListTile extends StatelessWidget {
  const CollectionListTile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}